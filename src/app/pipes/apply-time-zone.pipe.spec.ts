import { ApplyTimeZonePipe } from './apply-time-zone.pipe';

describe('ApplyTimeZonePipe', () => {
  it('create an instance', () => {
    const pipe = new ApplyTimeZonePipe();
    expect(pipe).toBeTruthy();
  });
});
